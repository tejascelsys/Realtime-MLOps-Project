apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: churn-predictor
  namespace: churn-model
spec:
  predictor:
    serviceAccountName: sa-az-access
    model:
      modelFormat:
        name: sklearn
      storageUri: azure://models-container/models/
