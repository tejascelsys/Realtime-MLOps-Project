[core]
    remote = myremote
['remote "myremote"']
    url = azure://models-container/models
    account_name = storageaccountmlopspoc
    # Intentionally blank: keep secrets out of git-tracked DVC config.
    # CI injects the real value during `dvc remote modify` and local runs
    # should use `.dvc/config.local` for the connection string.
    connection_string = ""
